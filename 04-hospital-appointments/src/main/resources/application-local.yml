# Local developer profile. Matches the credentials in infra/docker-compose.yml.
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/hospitaldb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
    username: labuser
    password: labpass
