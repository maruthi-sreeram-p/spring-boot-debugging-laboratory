# Local developer profile. Matches the credentials in infra/docker-compose.yml.
# Any other environment supplies MYSQL_* / REDIS_* through the environment instead.
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/shopdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: labuser
    password: labpass
  data:
    redis:
      host: localhost
      port: 6380
